<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Main Color -->
    <item>
        <shape android:shape="rectangle" android:dither="false" >
            <corners android:bottomLeftRadius="0dp" android:bottomRightRadius="0dp"/>
            <solid android:color="@color/colorTransLightGreen"/>
            <stroke android:width="0.5px" android:color="@color/colorCustomGreen" />
        </shape>
    </item>

</layer-list>